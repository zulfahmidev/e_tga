import 'package:flutter/material.dart';

class wisuda_mhs extends StatelessWidget {
  const wisuda_mhs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Wisuda'),
        centerTitle: true,
      ),
    );
  }
}
