import 'package:flutter/material.dart';

class proposal_mhs extends StatelessWidget {
  const proposal_mhs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Proposal'),
        centerTitle: true,
      ),
    );
  }
}
