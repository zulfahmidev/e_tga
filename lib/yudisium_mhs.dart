import 'package:flutter/material.dart';

class yudisium_mhs extends StatelessWidget {
  const yudisium_mhs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Yudisium'),
        centerTitle: true,
      ),
    );
  }
}
