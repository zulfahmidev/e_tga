import 'package:flutter/material.dart';

class dosPermintaanDos extends StatelessWidget {
  const dosPermintaanDos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Permintaan Dosen'),
        centerTitle: true,
      ),
    );
  }
}
