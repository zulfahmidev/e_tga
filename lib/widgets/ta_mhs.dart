import 'package:flutter/material.dart';

class ta_pnl extends StatelessWidget {
  const ta_pnl({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Tugas Akhir'),
        centerTitle: true,
      ),
    );
  }
}
