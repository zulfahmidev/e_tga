import 'package:flutter/material.dart';

class dosBimbingan extends StatelessWidget {
  const dosBimbingan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Bimbingan'),
        centerTitle: true,
      ),
    );
  }
}
