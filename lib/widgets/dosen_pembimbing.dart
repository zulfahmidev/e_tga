import 'package:flutter/material.dart';

class dosen_pembimbing extends StatelessWidget {
  const dosen_pembimbing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Dosen Pembimbing'),
        centerTitle: true,
      ),
      body: Column(),
    );
  }
}
