import 'package:flutter/material.dart';

class dosTA extends StatelessWidget {
  const dosTA({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow[600],
        title: Text('Tugas Akhir'),
        centerTitle: true,
      ),
    );
  }
}
